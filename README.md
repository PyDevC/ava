# newBrain

A personal knowledge base at the intersection of compilers and machine learning: how ML models become fast programs, and how they're built. ~250 markdown notes, all cross-linked as plain markdown.

- **[index.md](index.md)** — the map of the whole KB. Start here.
- Two spines: `Compilers/` (IR → LLVM → MLIR → DL compilers → the `torch.compile` stack → codegen) and `MachineLearning/` (algorithms → math → deep learning → NLP → vision → AutoML → evaluation → MLOps → RL), plus `PyTorch/`, `Programming/`, `OpenSource/`, `Roadmaps/` (career paths mapped to the notes), and `Store/` (reference).
- **[Roadmaps/where-am-i-now.md](Roadmaps/where-am-i-now.md)** — the self-assessment map.
